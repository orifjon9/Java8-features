Main class is provided that is essentially the same as the one used in example 6. Comments appear in the main method that indicate two queries that need to be executed. As in Problem 7, create a class LambdaLibrary that will store implementations of these queries as lambda pipeline expressions. Then call these expressions in the main method to verify they produce the expected results