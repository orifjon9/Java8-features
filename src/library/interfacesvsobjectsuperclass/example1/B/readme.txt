Employee had equals(Employee e) function which function parameter is exactly Employee.
It is different function with equals(Object o).
Java objects is use own function equals(Object o) when it compare

I override that equals(Object o) function.